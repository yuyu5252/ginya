<%= stylesheet_link_tag 'index', :media => "all" %>

<div class="foods-container" style="font-family: 'Yu Mincho', serif; color: #333; max-width: 1200px; margin: 0 auto; padding: 0 20px;">
  
  <div style="margin-top: 30px; margin-bottom: 40px;">
    <%= link_to "← トップページへ戻る", root_path, style: "color: #888; text-decoration: none; font-size: 14px; letter-spacing: 0.05em;" %>
  </div>

  <div class="news-header" style="text-align: center; margin-bottom: 60px;">
    <h2 class="news-title" style="font-size: 26px; font-weight: normal; letter-spacing: 0.15em; margin-bottom: 8px;">お知らせ一覧</h2>
    <p class="news-subtitle" style="font-size: 11px; color: #999; letter-spacing: 0.1em; transform: scale(0.9);">ALL NEWS</p>
  </div>

  <div class="news-grid-container">
    <% @foods.each do |food| %>
      <div class="news-grid-item">
        <%= link_to food_path(food), class: "news-grid-link" do %>
          
          <div class="news-grid-thumb">
            <% if food.image.attached? %>
              <%= image_tag food.image %>
            <% else %>
              <div class="no-image-box">NO IMAGE</div>
            <% end %>
          </div>

          <div class="news-grid-meta">
            <span class="news-grid-date"><%= food.created_at.strftime('%Y.%m.%d') %></span>
            <h3 class="news-grid-title"><%= food.title %></h3>
          </div>

        <% end %>
      </div>
    <% end %>
  </div>

</div>